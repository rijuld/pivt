/** Main workspace views — weather is fleet-scoped; other tabs often follow the selected load. */
export type WorkspaceTabId =
  | "overview"
  | "weather"
  | "routes"
  | "flow"
  | "drivers"
  | "log";
